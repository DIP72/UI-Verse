{
  "name": "ui-verse",
  "version": "1.0.0",
  "private": true,
  "description": "UI-Verse static site — quality gate tooling",
  "scripts": {
    "lint:js": "eslint '**/*.js' --ext .js",
    "lint:css": "stylelint '**/*.css'",
    "lint:html": "html-validate '**/*.html'",
    "link-check": "linkinator './index.html' --recursive --silent --summary",
    "quality": "npm run lint:html && npm run lint:css && npm run lint:js && npm run link-check"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "stylelint": "^15.0.0",
    "stylelint-config-standard": "^30.0.0",
    "html-validate": "^8.5.0",
    "linkinator": "^3.3.0"
  }
}
